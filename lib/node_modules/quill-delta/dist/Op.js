import { commonjsGlobal } from '../../../_virtual/_commonjsHelpers.js';
import { __exports as Op } from '../../../_virtual/Op.js';
import { __require as requireIterator } from './Iterator.js';

var hasRequiredOp;

function requireOp () {
	if (hasRequiredOp) return Op;
	hasRequiredOp = 1;
	var __importDefault = (commonjsGlobal && commonjsGlobal.__importDefault) || function (mod) {
	    return (mod && mod.__esModule) ? mod : { "default": mod };
	};
	Object.defineProperty(Op, "__esModule", { value: true });
	var Iterator_1 = __importDefault(requireIterator());
	var Op$1;
	(function (Op) {
	    function iterator(ops) {
	        return new Iterator_1.default(ops);
	    }
	    Op.iterator = iterator;
	    function length(op) {
	        if (typeof op.delete === 'number') {
	            return op.delete;
	        }
	        else if (typeof op.retain === 'number') {
	            return op.retain;
	        }
	        else {
	            return typeof op.insert === 'string' ? op.insert.length : 1;
	        }
	    }
	    Op.length = length;
	})(Op$1 || (Op$1 = {}));
	Op.default = Op$1;
	
	return Op;
}

export { requireOp as __require };
