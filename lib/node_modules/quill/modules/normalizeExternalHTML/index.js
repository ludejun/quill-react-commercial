'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var googleDocs = require('./normalizers/googleDocs.js');
var msWord = require('./normalizers/msWord.js');

const NORMALIZERS = [msWord.default, googleDocs.default];
const normalizeExternalHTML = doc => {
  if (doc.documentElement) {
    NORMALIZERS.forEach(normalize => {
      normalize(doc);
    });
  }
};

exports.default = normalizeExternalHTML;
