import normalize$1 from './normalizers/googleDocs.js';
import normalize from './normalizers/msWord.js';

const NORMALIZERS = [normalize, normalize$1];
const normalizeExternalHTML = doc => {
  if (doc.documentElement) {
    NORMALIZERS.forEach(normalize => {
      normalize(doc);
    });
  }
};

export { normalizeExternalHTML as default };
