'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _baseGet = require('./_baseGet.js');
var _baseSet = require('./_baseSet.js');
var _castPath = require('./_castPath.js');

/**
 * The base implementation of  `_.pickBy` without support for iteratee shorthands.
 *
 * @private
 * @param {Object} object The source object.
 * @param {string[]} paths The property paths to pick.
 * @param {Function} predicate The function invoked per property.
 * @returns {Object} Returns the new object.
 */
function basePickBy(object, paths, predicate) {
  var index = -1,
      length = paths.length,
      result = {};

  while (++index < length) {
    var path = paths[index],
        value = _baseGet.default(object, path);

    if (predicate(value, path)) {
      _baseSet.default(result, _castPath.default(path, object), value);
    }
  }
  return result;
}

exports.default = basePickBy;
